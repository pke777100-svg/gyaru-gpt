:root {
  --bg: #0a0a0a;
  --panel: #111;
  --text: #e5e7eb;
  --accent: #6366f1;
}

@media (prefers-color-scheme: light) {
  :root {
    --bg: #f9fafb;
    --panel: #fff;
    --text: #111827;
    --accent: #6366f1;
  }
}

body { margin:0; font-family:-apple-system; background:var(--bg); color:var(--text); }
header { text-align:center; padding:12px; border-bottom:1px solid #222; }
#chat { padding:12px; height:60vh; overflow-y:auto; }
.controls { display:flex; gap:8px; padding:12px; }
input { flex:1; padding:10px; border-radius:10px; border:none; background:var(--panel); color:var(--text); }
button { padding:10px 14px; border-radius:10px; border:none; background:var(--accent); color:#fff; font-weight:600; }
.msg { max-width:80%; margin-bottom:10px; padding:8px 12px; border-radius:12px; }
.black { background:var(--panel); border:1px solid #222; }
.user { background:var(--accent); color:#fff; margin-left:auto; }
.sub { font-size:12px; color:#9ca3af; }